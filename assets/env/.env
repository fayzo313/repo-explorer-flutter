# Base URL for the GitHub REST API (no auth required for public search,
# but rate-limited to 10 req/min unauthenticated). Add GITHUB_TOKEN to
# raise the limit to 30 req/min if you have one.
GITHUB_BASE_URL=https://api.github.com
GITHUB_TOKEN=
